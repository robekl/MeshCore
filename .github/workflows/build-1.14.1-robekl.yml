name: Build 1.14.1-robekl Release Artifacts

on:
  push:
    branches:
      - 1.14.1-robekl
  workflow_dispatch:

jobs:
  build-in-container:
    runs-on: [self-hosted, meshcore, ct141, release-builds]
    timeout-minutes: 720

    steps:
      - name: Run release build in container runner
        shell: bash
        run: |
          set -euxo pipefail
          /home/builder/meshcore-build-release.sh 1.14.1-robekl 1.14.1 1.14.1-robekl
